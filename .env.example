# Alchemy uses this region for every AWS resource and its S3 state store.
AWS_REGION=us-east-1

# Optional: choose a non-default AWS CLI profile before running `bun run infra:login`.
# AWS_PROFILE=personal
